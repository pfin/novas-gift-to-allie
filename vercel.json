{
  "buildCommand": "cd allie-app && npm install && npm run build",
  "outputDirectory": "allie-app/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/allie-app/$1"
    }
  ]
}